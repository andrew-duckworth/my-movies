import express from 'express'
//import { getConditions } from '../server'

const router = express.Router()

// router.get('./conditions', (req, res) => {
//   getConditions()
//     .then((conditions) => {
//       res.json(conditions)
//     })
//     .catch((err) => {
//       res.status(500).send(err.message)
//     })
// })

export default router
